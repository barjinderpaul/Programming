//Trees ; undirected graph, no cycle with unique path to every point(Node)
/*

1. Binary Tree :  ( Node can have atmost 2 childs )
	-> Binary search Tree; (Each left children less than parent and right children greater than parent  and every left subtree should be less than parent and every right subtree should be greater);
	-> Balance Binary Tree ( AVL );

	Traversals : 
	PreOrder, Post Order and Inorder
*/
