#include<bits/stdc++.h>
#define fast ios_base::sync_with_stdio(false);cin.tie(0);
using namespace std;
typedef long long ll;
int main(){
    ifstream iFile("input.txt");
    while (true) {
    int x;
    iFile >> x;
    if( iFile.eof() ) break;
    cout<<x<<"\n";
}
}
