#include<bits/stdc++.h>
#define pb push_back
using namespace std;
int main(){

    int q,l,r,n;
    vector<int>startingPoints,endingPoints;
    cin>>n>>q;
    while(q--){
        cin>>l>>r;
        startingPoints.pb(l);
        endingPoints.pb(r);
    }
    sort(startingPoints.begin(),sortingPoints.end());
    sort(endingPoints.begin(),endingPoints.end());
    int i=0,j=0;
    while(i)



}
